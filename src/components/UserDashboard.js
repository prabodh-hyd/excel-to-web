import React from 'react';

const UserDashboard = ({ currentUser }) => {
  return (
    <div>
      <h1>Welcome, {currentUser}</h1>
      {/* Add features for user dashboard here */}
    </div>
  );
};

export default UserDashboard;
